const age = 15; 

if( age >= 18) //Set condition 
{
    console.log(`Sy can start drving license 🚗`) //Execute code
}   else //If condtion is false perform this code
    {
        const yearsLeft = 18 - age;
        console.log(`Sy is too young, he has ${yearsLeft} years left before drivings license`);
    }

    const birthYear = 1996;
    let century;

    if (birthYear <= 2000)
    {
        century = 20;
    } else
        {
            century = 21;
        }
console.log(century);

